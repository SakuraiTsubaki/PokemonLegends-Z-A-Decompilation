# Repository Structure

Create purpose-based areas only when real work exists: `docs/`, `manifests/`, `src/`, `data/`, `assets/`, `tools/`, `tests/`, `patches/`. Platform-specific needs may extend locally. No structure-version roots, migration-history trees, or duplicate canonical homes.