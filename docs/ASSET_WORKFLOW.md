# Asset Workflow

Exact source → deterministic extraction/reconstruction → native identity/hashes → reviewable representation when useful → defined target conversion → byte/hash dedupe → manifest → round-trip validation → small batches. Cross-generation output keeps source identity and target contract.