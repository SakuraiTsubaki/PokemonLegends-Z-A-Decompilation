# Manifests

Track logical IDs, paths, target/version metadata, source location, size, hashes, generation method, verification state, dedupe relationships, and known differences. Unknown values stay explicit.